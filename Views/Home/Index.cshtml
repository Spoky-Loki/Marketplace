@{
    ViewData["Title"] = "Главная";
}
 @model HomeViewModel

<h1>Избранные продукты</h1>
<div class="row mt-5 mb-2">
	@{
		foreach (Product product in Model.favouriteProducts)
			@Html.Partial("AllProducts", product);
	}
</div>
